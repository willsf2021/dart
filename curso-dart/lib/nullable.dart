void returnText(){
  // Quando declaramos uma variável em atribuir valores NÃO PODEMOS UTILIZAR ESSA VARIÁVEL ENQUANTO NÃO ATRIBUIRMOS UM VALOR A ELA!
  // Ou seja, ela não será NULL!
  int? nume;
  // Ao menos que, utilizemos o ? após o tipo, assim sendo a variável pode ser usada e pode receber valor null durante seu lifecicle ou int.
  
  print(nume);
}