void returnText() {
  // Variáveis
  // tipo, nome, valor

  int prova =
      5; // declaração com atribuição de valor juntos ( declaração + atribuição = inicialização de variável)
  prova = 7; // reatribuição de valor
  int ex; // declaração apenas
  bool status = true;
  double oiroet = 3.2;
  String porov = "qwertyt";
  ex = 32; // atribuição de valor

  print(status);
  print(oiroet);
  print(porov);
  print(prova);
  print(ex);
}
